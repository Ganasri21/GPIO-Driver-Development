#include<stdint.h>

#define ADC_BASE_ADDR       0x40012000UL
#define ADC_CR1_REG_OFFSET  0x04
#define ADC_CR1_REG_ADDR    (ADC_BASE_ADDR + ADC_CR1_REG_OFFSET)

int main(){
	uint32_t *pAddCr1Reg = (uint32_t*) ADC_CR1_REG_ADDR;
	*pAddCr1Reg |= (1 << 8);
	for(;;);
}
